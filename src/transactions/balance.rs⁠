// src/transactions/balance.rs

pub struct BalanceInquiry {
    pub target_account_id: String,
}

impl BalanceInquiry {
    pub fn new(account_id: &str) -> Self {
        Self {
            target_account_id: account_id.to_string(),
        }
    }

    pub fn run_balance_check(&self) -> u32 {
        println!("[Ledger Query] Accessing routing file records for ID: {}", self.target_account_id);
        5250
    }
}

pub fn get_balance() -> f64 {
    // TODO: Implement actual balance retrieval logic for MonATM
    0.0
}

pub fn display_balance() {
    let current_balance = get_balance();
    println!("Current Balance: {:.2}", current_balance);
}
